package com.infovault.config;

// Imports @Autowired annotation to add dependencies into the class
import org.springframework.bean.factoty.annotation.Autowired;
// Imports @Bean to define beans in configuration class